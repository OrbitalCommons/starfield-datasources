[package]
name = "starfield-reflectance-library"
version = "0.1.0"
description = "Endmember surface reflectance spectra for resolved-body rendering, from the USGS Spectral Library"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
starfield = { workspace = true }
starfield-datasource-utils = { version = "0.1.0", path = "../datasource-utils" }
